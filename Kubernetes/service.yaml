apiVersion: v1
kind: Service
metadata:
  name: summarizer-service
  labels:
    app: summarizer
spec:
  type: NodePort
  ports:
    - name: "summarizer-service"
      port: 8000
      targetPort: 8000
      nodePort: 30080
  selector:
    app: summarizer